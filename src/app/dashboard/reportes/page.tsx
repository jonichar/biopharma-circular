"use client";
import { FileText } from "lucide-react";

export default function ReportesPage() {
  return (
    <div style={{ padding: "2rem", color: "var(--text-tertiary)", textAlign: "center" }}>
      <FileText size={48} style={{ margin: "0 auto 1rem", opacity: 0.5 }} />
      <h2 style={{ color: "var(--text-primary)", fontSize: "1.5rem", marginBottom: "0.5rem" }}>Tus Reportes</h2>
      <p>Página en construcción. Aquí generaremos certificados de tus contribuciones circulares.</p>
    </div>
  );
}
